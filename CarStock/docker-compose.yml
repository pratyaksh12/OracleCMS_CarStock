version: '3.8'

services:
  carstock-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5237:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      # Mount the local database so it persists across container restarts
      - ./database.db:/app/database.db
